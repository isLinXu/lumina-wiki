---
title: "FlashAttention: Fast and Memory-Efficient Exact Attention"
type: note
created: 2026-04-06T03:52:06Z
source: raw/2026-04-06/FlashAttention_Paper.md
status: seed
---

# FlashAttention: Fast and Memory-Efficient Exact Attention

> 💡 **摘要**
> # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...

| 属性 | 值 |
|------|-----|
| 类型 | note |
| 来源 | `raw/2026-04-06/FlashAttention_Paper.md` |
| 编译时间 | 2026-04-06 |

---

*由 **Lumina Compiler** 于 2026-04-06T03:52:06Z 自动生成*
*来源: `raw/2026-04-06/FlashAttention_Paper.md`*

---

**▸ 更新于 2026-04-27T00:51:33Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-04-28T00:53:39Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-04-29T00:54:43Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-04-30T00:54:33Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-01T00:58:17Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-02T00:52:07Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-03T00:56:27Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-04T00:54:41Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-05T00:50:30Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-06T00:49:39Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-07T00:54:21Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-08T00:53:55Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-09T00:54:05Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-10T00:57:41Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-11T00:58:06Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-12T00:53:32Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-13T00:57:30Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-22T01:00:55Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-23T00:58:36Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-24T01:01:48Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-25T01:03:04Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-26T00:59:21Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-27T01:02:02Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-28T00:56:28Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-29T01:04:57Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-30T00:59:52Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-05-31T01:06:02Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-01T01:09:09Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-02T01:07:47Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-03T01:14:10Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-04T01:14:18Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-05T01:05:14Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-06T01:01:23Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-07T01:08:20Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-08T01:08:34Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-09T00:59:54Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-10T01:06:51Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-11T01:07:12Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-12T01:10:32Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-13T01:08:35Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-14T01:10:20Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-15T01:12:50Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-16T01:15:30Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-17T01:10:07Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-18T01:09:36Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-19T01:15:19Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-20T01:04:37Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-21T01:12:07Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-22T01:11:44Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-25T01:01:56Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-26T01:04:49Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-27T01:00:32Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-28T01:05:09Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-29T01:06:01Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-06-30T01:01:55Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...



---

**▸ 更新于 2026-07-01T01:06:16Z**
> 来源: `raw/2026-04-06/FlashAttention_Paper.md`

**补充摘要**: # FlashAttention: Fast and Memory-Efficient Exact Attention

## 原始内容

FlashAttention 是一种 IO 感知的精确注意力算法，通过分块计算（tiling）和重计算（recomputation）策略，显著降低了标准 Self-Attention 的内存占用，同时保持数学等价性。

### 核心思想

1. **IO 感知 (IO-Aware)**：注意力计算的瓶颈不在 FLOPs，而在 GPU 内存层级之间的数据搬运（HBM ↔ SRAM）。FlashAttention 最小化了 HBM 读写次数。

2. **分块...

